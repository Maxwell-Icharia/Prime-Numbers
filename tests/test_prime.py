import unittest
from main.prime import prime_num


def test_is_prime(self):
	self.assertEqual(self.prime_num(6), 6)
	self.assertEqual(4, 1)

def test_list(self):
	self.assertIsinstance(i, list)

def test_is_string(self):
	x = 5
	if x == int:
		self.assertTrue(x, int)
		v = str(x)
		self.assertTrue(v, str)

if __name__ == '__main__':
	unittest.main()